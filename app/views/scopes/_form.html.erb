<%= form_with(model: scope, local: true) do |form| %>
  <% if scope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scope.errors.count, "error") %> prohibited this scope from being saved:</h2>

      <ul>
      <% scope.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scope_type %>
    <%= form.text_field :scope_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
